<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="us.bibos.notifier" >
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="us.bibos.notifier.Activities.HomeActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="us.bibos.notifier.Activities.HuntingActivity"
            android:label="@string/hunting_activity"
            android:parentActivityName="us.bibos.notifier.Activities.HomeActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="us.bibos.notifier.Activities.HomeActivity"/>
        </activity>
        <service
            android:name="us.bibos.notifier.Services.PageProbingService"
            android:exported="false"/>
    </application>

</manifest>
